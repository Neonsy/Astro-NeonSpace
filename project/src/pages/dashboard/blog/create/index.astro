---
import { SignOutButton } from '@clerk/astro/components';

const user = await Astro.locals.currentUser();

const isAuthenticated = user?.publicMetadata.isPrivileged;

if (!isAuthenticated) {
    return Astro.redirect('/');
}
---

<h1>Create Blog</h1>

<SignOutButton />
