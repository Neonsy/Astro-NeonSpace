---
export const prerender = true;

const user = await Astro.locals.currentUser();

const isAuthenticated = user?.privateMetadata.role === 'owner';

if (!isAuthenticated) {
    return Astro.redirect('/wip');
}
---
