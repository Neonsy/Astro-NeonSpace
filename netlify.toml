[build]
  directory = "./project"
  command = "pnpm build"
  functions = "netlify/functions"
  publish = "dist"

[check]
  directory = "./project"
  command = "pnpm build:workflow"
  functions = "netlify/functions"
  publish = "dist"